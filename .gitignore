.DS_Store
*.bak
*.tmp

# Claude Code local settings (per-user permissions, paths)
.claude/settings.local.json

# Go build artefacts
bin/memoryctl
bin/memoryctl.exe
coverage.out

# Runtime artefacts from ~/.claude/memory/. Not expected to appear here,
# but cheap insurance if a user ever version-controls their memory dir.
.wal
.wal.*
.analytics-report
.tfidf-index
.tfidf-index.*
index.db
self-profile.md
.runtime/
.cache/
